<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:integration="http://www.springframework.org/schema/integration"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd">

  <integration:gateway service-interface="com.oreilly.integration.PrinterGateway"
                       default-request-channel="inputChannel"></integration:gateway>


  <integration:channel id="inputChannel"/>
  <integration:channel id="outputChannel"/>
  <integration:channel id="filterChannel"/>
  <integration:channel id="enricherChannel"/>

  <integration:header-enricher input-channel="inputChannel" output-channel="enricherChannel">
    <integration:header name="publicKey" ref="customHeaderEnricher" method="getHeaderValue"></integration:header>
  </integration:header-enricher>
  <integration:header-filter header-names="privateKey, someotherName" input-channel="enricherChannel"
                             output-channel="filterChannel"/>

  <integration:object-to-json-transformer input-channel="filterChannel"
                                          output-channel="outputChannel"></integration:object-to-json-transformer>

  <bean class="com.oreilly.integration.CustomHeaderEnricher" id="customHeaderEnricher"/>
  <bean class="com.oreilly.integration.PrintService" id="printService"/>

  <integration:service-activator ref="printService" method="print" input-channel="outputChannel"/>

</beans>